name: Self-Hosted Runner Test

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: [self-hosted, ARM64]
    steps:
      - name: ✅ 打印主机名
        run: hostname

      - name: 🐧 查看系统信息
        run: uname -a

      - name: 📂 查看当前目录
        run: ls -alh
